appId: sc4pac-auth

files:
  - "!cli/**"
  - "!webapp/**"
  - "!dist/**"

extraFiles:
  - webapp
  - cli

win:
  target:
    - zip
  sign: false

mac:
  target: dmg

linux:
  target:
    - zip

directories:
  output: dist/${platform}
